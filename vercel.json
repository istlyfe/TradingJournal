{
  "buildCommand": "npm install --save-dev typescript @types/react @types/node && npm install papaparse axios @radix-ui/react-tabs --force && next build --no-lint",
  "outputDirectory": ".next",
  "devCommand": "next dev",
  "framework": "nextjs"
} 